<form method="post" action="{{ path('app_main_fournisseur_delete', {'id': fournisseur.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce fournisseur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fournisseur.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
