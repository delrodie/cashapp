{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fournisseur.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ fournisseur.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fournisseur.nom }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ fournisseur.contact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_main_fournisseur_index') }}">back to list</a>

    <a href="{{ path('app_main_fournisseur_edit', {'id': fournisseur.id}) }}">edit</a>

    {{ include('main/fournisseur/_delete_form.html.twig') }}
{% endblock %}
