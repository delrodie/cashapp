{% extends 'base.html.twig' %}

{% block title %}Cloud{% endblock %}

{% block body %}
    <h1>Cloud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cloud.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ cloud.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_main_cloud_index') }}">back to list</a>

    <a href="{{ path('app_main_cloud_edit', {'id': cloud.id}) }}">edit</a>

    {{ include('main/cloud/_delete_form.html.twig') }}
{% endblock %}
